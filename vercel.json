{
  "version": 2,
  "builds": [
    { "src": "src/index.html", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "www"
}

